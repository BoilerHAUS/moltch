{
  "name": "@moltch/policy-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Canonical policy-decision state machine for moltch",
  "scripts": {
    "lint": "node scripts/lint.mjs",
    "test": "node --test test/*.test.mjs",
    "build": "node scripts/build.mjs",
    "build:simulation": "node scripts/run-decision-loop-simulation.mjs --write",
    "check:simulation": "node scripts/run-decision-loop-simulation.mjs",
    "check:reason-codes": "node scripts/check-reason-code-drift.mjs",
    "check:migrations": "node scripts/check-reason-code-migrations.mjs",
    "check": "npm run lint && npm run test && npm run build && npm run check:simulation && npm run check:reason-codes && npm run check:migrations"
  }
}
