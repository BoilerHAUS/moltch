{
  "name": "@moltch/policy-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Canonical policy-decision state machine for moltch",
  "scripts": {
    "lint": "node scripts/lint.mjs",
    "test": "node --test test/*.test.mjs",
    "build": "node scripts/build.mjs",
    "check": "npm run lint && npm run test && npm run build"
  }
}
